import { Layout } from '@/shared/ui/layout'
import { LoginContent } from './login-content'

export const HomePage = () => {
    return (
        <Layout>
            <LoginContent />
        </Layout>
    )
}
